<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login – AlUla</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background-image: url("https://www.sellingtravel.co.uk/wp-content/uploads/2023/10/Oasis-Heritage-Trail-lead.jpg");
      background-size: cover;
      background-position: center;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
    }

    .overlay {
      position: absolute;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: rgba(0, 0, 0, 0.6);
      z-index: 0;
    }

    .container {
      position: relative;
      z-index: 1;
      display: flex;
      width: 80%;
      max-width: 1000px;
      background-color: rgba(26, 26, 26, 0.85);
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 25px rgba(0,0,0,0.4);
    }

    .left-side {
      flex: 1;
      padding: 40px;
      color: #d4af37;
    }

    .left-side h1 {
      font-size: 36px;
      margin-bottom: 20px;
    }

    .left-side p {
      font-size: 16px;
      line-height: 1.6;
      color: #ccc;
    }

    .login-box {
      flex: 1;
      background-color: #fff;
      color: #3B2A24;
      padding: 40px;
      text-align: center;
    }

    .login-box h2 {
      margin-bottom: 20px;
      color: #d4af37;
    }

    .login-box input {
      width: 100%;
      padding: 12px;
      margin: 15px 0;
      border: none;
      border-bottom: 1px solid #ccc;
      background-color: transparent;
      color: #3B2A24;
      font-size: 14px;
    }

    .login-box input::placeholder {
      color: #999;
    }

    .login-box button {
      margin-top: 20px;
      background-color: #3B2A24;
      color: white;
      border: none;
      padding: 12px 22px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
    }

    .login-box button:hover {
      background-color: #5a3d30;
    }

    .login-box a {
      color: #d4af37;
      text-decoration: none;
    }

    .login-box a:hover {
      text-decoration: underline;
    }

    .error {
      color: red;
      font-size: 14px;
    }

    @media(max-width: 768px) {
      .container {
        flex-direction: column;
      }
      .left-side, .login-box {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="overlay"></div>
  <div class="container">
    <div class="left-side">
      <h1>EXPLORE ALULA</h1>
      <p>Where heritage meets adventure.<br>
      Discover timeless landscapes, ancient wonders, and unforgettable experiences.<br>
      Your journey begins here.</p>
    </div>
    <div class="login-box">
      <h2>Login</h2>
      <% if(err_msg){ %>
        <p class="error"><%= err_msg %></p>
      <% } %>
      <form action="/login" method="POST">
        <input type="email" name="email" placeholder="Email" value="<%= data.email || '' %>" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
      </form>
      <p>Don't have an account? <a href="/signup">Sign up</a></p>
    </div>
  </div>
</body>
</html>







